pipeline {
    agent any
    tools {
        maven 'M2_HOME'
    }
    stages{
        stage('maven commands'){
            steps{
                sh 'mavn clean compile install package'
            }
        }
    }
}